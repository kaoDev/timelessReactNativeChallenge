export const NO_ASSETS_MODAL = {
  title: 'Empty Assets',
  description: 'Looks like currently no assets available for the selection.',
} as const;

export const ERROR_MODAL = {
  title: 'No Connection',
  description: 'Connection to the server failed. Please try again shortly.',
} as const;

export const LOADING = 'Loading...' as const;
export const DROPS = 'Drops' as const;
export const TRY_AGAIN = 'Try Again' as const;
export const DROP_MARKET_VALUE = 'Drop Market Value' as const;
export const OUR_DROP_PRICE = 'Our Drop Price' as const;
