import colorsModule from "./colors";
import paletteModule from "./palette";
import stringsModule from "./strings";

export const colors = colorsModule;
export const palette = paletteModule;
export const strings = stringsModule;

const R = {
  colors,
  palette,
  strings,
};

export default R;
